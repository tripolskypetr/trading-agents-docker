[project]
name = "trading-agents-docker"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "tradingagents",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "ccxt>=4.0.0",
]

[tool.uv.sources]
tradingagents = { path = "./src/agent", editable = true }
